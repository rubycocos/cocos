<!DOCTYPE html>
<html>
<head>
  <meta charset='utf-8'>
  <title>DB Browser</title>
  
  <link href="<%= url('/style.css') %>" rel='stylesheet'>
  
  <script src="<%= url('/jquery-2.0.3.min.js') %>"></script>
  <script src="<%= url('/jquery.tabs.js') %>"></script>
</head>
<body>

<p>
  <%= link_to 'DB Browser', root_path(), class: 'nav' %> |
  <% ActiveRecord::Base.configurations.each do |key,hash| %>
     <%= link_to "#{key}(#{hash[:database]})", db_path( key ) %>
  <% end %>
</p>

<%= yield %>

<%= erb :'shared/_version' %>

</body>
</html>